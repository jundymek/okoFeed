import React, { ReactElement } from "react";

const Header = (): ReactElement => {
  return (
    <header className="h-10 bg-gray-500 flex items-center">
      <h1>Oko Feed</h1>
    </header>
  );
};

export default Header;
